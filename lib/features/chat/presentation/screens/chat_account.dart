import 'package:flutter/material.dart';

class ChatAccount extends StatelessWidget {
  const ChatAccount({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Chat Account')), body: Center(child: Text('Chat Account')));
  }
}
